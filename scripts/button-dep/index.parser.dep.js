// import TagManager from "./../tag-manager";

// class ButtonParser {
//   static parse = content => {
//     const tagRegex = new RegExp(`\\[button\\].*?\\[\\/button\\]`, "gs");

//     const tags = TagManager.getAll(tagRegex, content);

//     return tags;
//   };

//   static parseConfig = tag => {
//     const regex = new RegExp(`\\[button\\](.*?)\\[\\/button\\]`, "gs");

//     const content = TagManager.get(regex, tag);

//     return JSON.parse(content);
//   };
// }

// export default ButtonParser;
